## What & why

<!-- The problem this solves and the approach taken. -->

## How verified

<!-- Tests added/updated, commands run (pnpm test / pnpm test:coverage), manual checks. -->

## Checklist

- [ ] `pnpm test` green (and `pnpm test:coverage` when behavior changed)
- [ ] CHANGELOG / README updated when user-visible behavior or docs change
- [ ] No secrets or machine-local files (`harness-paths.json` / `tsconfig.paths.json` are gitignored)
