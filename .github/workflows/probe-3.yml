name: probe-3

# Comment between the name and the on block, like the failing files have.
# Second comment line with a colon: inside the text.

on: push

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - run: echo probe-3-ok
